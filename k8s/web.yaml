apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: miamo
  labels:
    app: miamo
    service: web
spec:
  type: ClusterIP
  selector:
    app: miamo
    service: web
  ports:
    - port: 3100
      targetPort: 3100
      protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: miamo
  labels:
    app: miamo
    service: web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: miamo
      service: web
  template:
    metadata:
      labels:
        app: miamo
        service: web
    spec:
      containers:
        - name: web
          image: miamo-web:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3100
          envFrom:
            - configMapRef:
                name: miamo-config
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3100
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 3100
            initialDelaySeconds: 5
            periodSeconds: 5
