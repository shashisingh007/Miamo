# ─── Miamo Auth Service K8s ───────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: miamo-auth
  namespace: miamo
  labels:
    app: miamo-auth
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: miamo-auth
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: miamo-auth
        tier: backend
    spec:
      containers:
        - name: auth
          image: miamo-auth:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3201
          env:
            - name: PORT
              value: "3201"
          envFrom:
            - configMapRef:
                name: miamo-config
            - secretRef:
                name: miamo-secrets
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3201
            initialDelaySeconds: 20
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /ready
              port: 3201
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: miamo-auth
  namespace: miamo
spec:
  selector:
    app: miamo-auth
  ports:
    - port: 3201
      targetPort: 3201
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: miamo-auth-hpa
  namespace: miamo
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: miamo-auth
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
