# ─── Miamo User Service K8s ───────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: miamo-users
  namespace: miamo
  labels:
    app: miamo-users
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: miamo-users
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: miamo-users
        tier: backend
    spec:
      containers:
        - name: users
          image: miamo-users:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3202
          env:
            - name: PORT
              value: "3202"
          envFrom:
            - configMapRef:
                name: miamo-config
            - secretRef:
                name: miamo-secrets
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3202
            initialDelaySeconds: 20
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /ready
              port: 3202
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: miamo-users
  namespace: miamo
spec:
  selector:
    app: miamo-users
  ports:
    - port: 3202
      targetPort: 3202
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: miamo-users-hpa
  namespace: miamo
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: miamo-users
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
