apiVersion: v1
kind: Service
metadata:
  name: content
  namespace: miamo
  labels:
    app: miamo
    service: content
spec:
  type: ClusterIP
  selector:
    app: miamo
    service: content
  ports:
    - port: 3205
      targetPort: 3205
      protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: content
  namespace: miamo
  labels:
    app: miamo
    service: content
spec:
  replicas: 2
  selector:
    matchLabels:
      app: miamo
      service: content
  template:
    metadata:
      labels:
        app: miamo
        service: content
    spec:
      containers:
        - name: content
          image: miamo-content:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3205
          envFrom:
            - configMapRef:
                name: miamo-config
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3205
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 3205
            initialDelaySeconds: 5
            periodSeconds: 5
