apiVersion: v1
kind: Service
metadata:
  name: messaging
  namespace: miamo
  labels:
    app: miamo
    service: messaging
spec:
  type: ClusterIP
  selector:
    app: miamo
    service: messaging
  ports:
    - port: 3204
      targetPort: 3204
      protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: messaging
  namespace: miamo
  labels:
    app: miamo
    service: messaging
spec:
  replicas: 2
  selector:
    matchLabels:
      app: miamo
      service: messaging
  template:
    metadata:
      labels:
        app: miamo
        service: messaging
    spec:
      containers:
        - name: messaging
          image: miamo-messaging:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3204
          envFrom:
            - configMapRef:
                name: miamo-config
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3204
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 3204
            initialDelaySeconds: 5
            periodSeconds: 5
