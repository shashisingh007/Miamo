apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: miamo
  labels:
    app: miamo
    service: auth
spec:
  type: ClusterIP
  selector:
    app: miamo
    service: auth
  ports:
    - port: 3201
      targetPort: 3201
      protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth
  namespace: miamo
  labels:
    app: miamo
    service: auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: miamo
      service: auth
  template:
    metadata:
      labels:
        app: miamo
        service: auth
    spec:
      containers:
        - name: auth
          image: miamo-auth:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3201
          envFrom:
            - configMapRef:
                name: miamo-config
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "250m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3201
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3201
            initialDelaySeconds: 5
            periodSeconds: 5
